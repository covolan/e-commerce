import React from "react";
import HeroComponentTitle from "./HeroComponentTitle";

function HeroComponentLeft({ children }) {
  return <div className="hero-left">{children}</div>;
}

export default HeroComponentLeft;
